# Sessione 2026-04-26 — Billing v4 + Stripe Connect Direct Charge

**Stato finale**: TUTTO COMPLETATO E LIVE. Branch main pulito. Vercel auto-deploy in produzione.

## Commits (8 nuovi, in ordine cronologico)

1. `1e078c8` `feat(billing): wiring accrueCommission + cron agency-payouts` — v2
2. `12a1eed` `feat(billing): Stripe Connect Express tenant — Fase 1 onboarding` — v3
3. `547e8e6` `feat(billing): Direct Charge end-to-end · Fasi 2-6 Stripe Connect` — v4
4. `48e087a` `fix(billing): rename rate_plans → payment_rate_plans (collision with legacy)`
5. `2ae15d3` `docs(billing): split corrente vs storico`
6. `2188b4c` `docs(billing): presentazione dinamica + guida partner`
7. `511c35f` `chore(billing): audio MP3 voice-over generati via OpenAI TTS`
8. `c0279cf` `feat(skills): create-presentation skill scoped TouraCore`

## Migration cloud applicate

- `00148_agency_payouts` — ledger payout mensili Stripe Connect Transfer
- `00149_tenants_stripe_connect` — colonne stripe_connect_* su tenants
- `00150_rate_plans_and_payments` — payment_rate_plans + reservation_payment_methods/attempts + colonne payment_state

Verificate via Management API. Tutte LIVE.

## Cosa è ora possibile fare in produzione

1. **Tenant onboarding Stripe**: `/[tenantSlug]/settings/payments` → click "Collega Stripe" → 5min KYC + IBAN.
2. **Configurare tariffe**: `/[tenantSlug]/{stays,dine,rides,activities}/[entitySlug]/rate-plans` → 4 tipi (free_cancellation/deposit_30/partially_refundable_50/non_refundable). Auto-seed default alla prima visita.
3. **Pubblicazione gating**: entity senza `charges_enabled=true` bloccata con errore chiaro.
4. **Checkout pubblici Direct Charge** funzionante per 4 verticali: cliente paga direttamente al Connect account tenant, application_fee a TouraCore, split alla fonte.
5. **Cron auto-charge daily 03:00 UTC**: addebito differito carta salvata.
6. **Cron auto-cancel daily 04:00 UTC**: libera slot dopo fail terminale.
7. **Cron agency-payouts mensile** (legacy commission accrual pre-Connect).
8. **Magic link aggiorna carta**: cliente non-loggato riceve URL → redirect Stripe Customer Portal nativo.
9. **Capture action**: server action pronta (manca solo bottone UI).

## v5 — Cosa resta da fare (prossima sessione)

| # | Task | Stima | Priorità |
|---|------|-------|----------|
| 1 | SetupIntent al booking per `free_cancellation` (no charge immediato) | 2h | P0 |
| 2 | Selettore rate plan nel widget pubblico × 4 verticali | 3h | P0 |
| 3 | Bottone "Cattura ora" nelle UI prenotazioni × 4 verticali | 2h | P1 |
| 4 | Email cliente automatic su payment_failed con magic link | 2h | P1 |
| 5 | Test E2E Stripe test mode (success + insufficient_funds + expired_card) | 3h | P2 |

**Totale stima v5**: ~12h dev + buffer.

## Documentazione creata

- `docs/billing/billing-system.html` (v4 corrente, calcolatore live)
- `docs/billing/billing-system-history.html` (storico v1→v4)
- `docs/billing/billing-modifications-report.md` (report tecnico v4)
- `docs/billing/guida-partner.html` (guida testuale stampabile)
- `docs/billing/presentation/index.html` (11 slide animate + audio)
- `docs/billing/presentation/audio/*.mp3` (voice-over OpenAI TTS)
- `docs/billing/presentation/generate-audio.mjs` (script ri-generazione)

## Skill custom registrata

- `.claude/skills/create-presentation/SKILL.md` con templates completi
- Trigger naturale: "crea presentazione X", "guida per agenzie Y"
- Output: presentazione + guida + audio nello stile esatto billing v4

## Env propagation

- `OPENAI_API_KEY` salvata in `apps/web/.env.local` + Vercel production + development (preview skipped, CLI interactive).
- Tutte le altre env restano invariate.

## Stato verifiche

- `pnpm typecheck` → 17/17 PASS
- `pnpm lint` → 0 errors (19 warnings preesistenti)
- Test E2E Playwright NON eseguiti questa sessione

## Per riprendere in nuova chat

1. Leggere `MEMORY.md` (auto)
2. Leggere `billing_v4_complete.md` (auto)
3. Leggere questo file `docs/SESSION-2026-04-26.md`
4. Iniziare da v5 task list sopra. P0 = SetupIntent + selettore widget pubblico.

## Riferimenti rapidi

- Documentazione billing: `docs/billing/`
- Skill presentazioni: `.claude/skills/create-presentation/`
- Migrations cloud applicate: `00148`, `00149`, `00150`
- Helper Stripe Connect: `packages/core/billing/src/connect-charge.ts`
- Helper rate plans: `packages/core/billing/src/rate-plans.ts`
- Helper magic link: `packages/core/billing/src/magic-link.ts`
- Wiring accrue commission: `packages/core/agency/src/wiring.ts`
