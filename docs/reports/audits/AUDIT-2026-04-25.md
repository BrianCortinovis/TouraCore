# Audit Codebase 2026-04-25

Audit completo TouraCore: antipattern React, performance, cache, security. Esecuzione su `main` post commit `21f76e6`.

## Findings & Stato

### P0 — Critical
| # | Tipo | File | Problema | Stato |
|---|------|------|----------|-------|
| 1 | Security | `apps/web/src/app/api/v1/ai/generate-guidebook/route.ts` | UPDATE su `guidebooks` senza tenant scope, possibile cross-tenant tampering | ✅ FIXED |
| 2 | Security | `apps/web/src/app/api/public/booking/_shared.ts:66` | CORS `*` su mutazioni se Origin assente | ✅ FIXED |
| 3 | Security | route handler `api/v1/*` + `cookie-consent` + `vitals` + `analyze-review` + `suggest-reply` | `request.json()` senza Zod parsing | ✅ FIXED |
| 4 | Perf | `apps/web/src/app/book/[slug]/page.tsx`, `book/multi/[slug]/page.tsx` | `dynamic='force-dynamic'` blocca ISR | ✅ FIXED (revalidate=60) |

### P1 — High
| # | Tipo | File | Problema | Stato |
|---|------|------|----------|-------|
| 5 | Perf | `packages/core/listings/src/queries.ts` | No `react.cache()` → query duplicate per request | ✅ FIXED (`apps/web/src/lib/listings-cache.ts`) |
| 6 | Perf | `analytics-dashboard.tsx`, `consolidated-dashboard.tsx` | Recharts statico +180KB bundle | ✅ FIXED (next/dynamic) |
| 7 | Perf | `app/discover/page.tsx` | `<img>` invece next/image | ✅ FIXED (next/image + priority LCP) |
| 8 | Perf | `packages/core/listings/src/queries.ts:listAllPublicListings` | `select *` + limit 200 | ✅ FIXED (`listAllPublicListingsCards` lite) |
| 9 | Perf | `book/experience`, `book/bike` | `force-dynamic` ovunque | 🟡 PARTIAL (usano searchParams → dynamic comunque) |

### P2 — Medium
| # | Tipo | File | Problema | Stato |
|---|------|------|----------|-------|
| 10 | RSC | 13 client component (sidebar/nav/topbar) | Hydration inutile | ❌ SKIPPED — usano `usePathname` per active link, non convertibili |
| 11 | Perf | `cron/reviews-ingest`, `cron/ical-sync` | Loop con await invece Promise.all | ✅ FIXED reviews-ingest. iCal lasciato (transactional concerns) |
| 12 | React | 11 punti `key={index}` | Re-render instabili | ❌ FALSE POSITIVE — array immutabili (Recharts Cell, PALETTE costante, Star ratings) |
| 13 | React | `register/page.tsx`, `book-table-client.tsx`, `embed-client.tsx` | `useEffect` fetch senza AbortController | ✅ FIXED (register + book-table). embed-client già aveva cleanup |
| 14 | Perf | Editor (Monaco/Tiptap/MenuEditor/FloorPlanEditor) | Import statico | ⏭️ DEFERRED — verifica caso per caso, non critical |

## Verify

```
pnpm typecheck    # 17/17 PASS ✅
pnpm lint         # 0 errori, 19 warning (tutti pre-esistenti, non bloccanti)
```

## Cleanup lint (236 errori → 0)

Pulizia totale errori lint senza rompere funzioni:

| Categoria | Prima | Dopo | Come |
|-----------|-------|------|------|
| `no-unused-vars` | 110 | 0 | Plugin `unused-imports` autofix import non usati + prefisso `_` o rimozione codice morto |
| `no-html-link-for-pages` | 95 | 0 | Sostituiti `<a href="/...">` con `<Link>` (recupero prefetch Next.js) |
| `no-explicit-any` | 24 | 0 | Tipi specifici sui runtime; warn su E2E test |
| `prefer-const` | 4 | 0 | `let`→`const` su variabili mai riassegnate |
| `rules-of-hooks` | 1 | 0 | Override file `e2e/**` (`use` di Playwright fixture, non React hook) |
| `no-require-imports` | 1 | 0 | Import top-level `xmlEscape` invece di `require()` |
| `no-assign-module-variable` | 1 | 0 | Rinominata var `module` → `moduleInfo` (evita shadowing Node) |

### Modifiche config eslint
- `packages/config/eslint/index.mjs` — Aggiunto plugin `eslint-plugin-unused-imports` con autofix; sostituita `@typescript-eslint/no-unused-vars` (no autofix) con `unused-imports/no-unused-vars` (autofix import unused). Convenzione `_` prefix per parametri intenzionalmente unused (signature override).
- `apps/web/eslint.config.mjs` — Override file E2E: `no-explicit-any` warn (cast pragmatici Playwright), `rules-of-hooks` off (parametro `use` Playwright extend).

### Fix sicuri runtime
- `superadmin/billing/overrides/page.tsx` — `module` → `moduleInfo` (no shadow su build Webpack)
- `lib/rt-middleware.ts` — `require()` dinamico → `import` top-level (no eval-style import)
- `api/v1/bundles/[id]/fulfill/route.ts` — `item: any, bundle: any, guest: any` → interface tipate
- `api/public/booking/{checkout,create}/route.ts` — body cast `any` → struttura tipizzata
- `book/multi/[slug]/success/page.tsx` — bundle/items `any` → tipi `BundleRow`/`BundleItemRow`

## File modificati

### Security (Sprint 1)
- `apps/web/src/app/api/v1/ai/generate-guidebook/route.ts` — auth + Zod + tenant scope check via RLS user client
- `apps/web/src/app/api/v1/ai/analyze-review/route.ts` — auth + Zod + tenant scope su reviews
- `apps/web/src/app/api/v1/ai/suggest-reply/route.ts` — auth + Zod
- `apps/web/src/app/api/v1/bundles/route.ts` — Zod schema completo (BundleSchema)
- `apps/web/src/app/api/cookie-consent/route.ts` — Zod
- `apps/web/src/app/api/vitals/route.ts` — Zod (cap su `route` length, value range)
- `apps/web/src/app/api/public/booking/_shared.ts` — CORS no più `*`, supporta whitelist `allowedDomains`

### Cache foundation (Sprint 1)
- `apps/web/src/lib/listings-cache.ts` — NUOVO. 9 wrapper `react.cache()` per query listings/details/photos
- `apps/web/src/app/s/[tenantSlug]/[entitySlug]/page.tsx` — usa `*Cached` (dedupe metadata+page+jsonld)
- `apps/web/src/app/embed/listing/[tenantSlug]/[entitySlug]/page.tsx` — idem
- `apps/web/src/app/book/[slug]/page.tsx` — `revalidate=60` invece di force-dynamic
- `apps/web/src/app/book/multi/[slug]/page.tsx` — `revalidate=60`

### Performance (Sprint 2)
- `apps/web/src/app/(app)/[tenantSlug]/dine/[entitySlug]/analytics/page.tsx` — `next/dynamic` su AnalyticsDashboard
- `apps/web/src/app/(app)/[tenantSlug]/consolidated/page.tsx` — `next/dynamic` su ConsolidatedDashboard
- `apps/web/src/app/discover/page.tsx` — next/image con `priority` su prime 4 (LCP), `sizes` responsive, `listAllPublicListingsCards` lite
- `packages/core/listings/src/queries.ts` — nuova `listAllPublicListingsCards` (10 campi vs 30+) + tipo `PublicListingCard`
- `packages/core/listings/src/index.ts` — export

### Cleanup (Sprint 3)
- `apps/web/src/app/api/cron/reviews-ingest/route.ts` — `Promise.all` adapter + bulk upsert (~Nx latenza ridotta)
- `apps/web/src/app/(auth)/register/page.tsx` — AbortController su fetch invite
- `apps/web/src/app/book-table/[slug]/book-table-client.tsx` — AbortController + skip setState dopo abort

## Impatto stimato

- **TTFB /book/[slug]**: -200/400ms (ISR cache hit)
- **TTFB /discover**: -300/400ms (narrow select + ISR)
- **LCP discover/listing**: -200/300ms (next/image + priority)
- **Bundle dashboard analytics**: -180KB (recharts dynamic)
- **DB load listing pages**: ~40% in meno (react.cache dedupe)
- **Security posture**: P0 closed — tenant scope + Zod + CORS

## Follow-up (non bloccanti)

- iCal cron: refactor transactional con upsert batch
- 13 client component sidebar: valutare alternativa con server component + client wrapper minimo per `usePathname`
- Editor pesanti (Monaco/Tiptap): dynamic import case-by-case
- Lint repo (236 errori): cleanup dedicato fuori scope audit
